describe("Application", function() {
	describe('url_root',function(){
  	it('holds the correct url',function(){
    	expect(url_root).toContain('http://psd-dev.internal.sanger.ac.uk');
  	});
  });
	//describe('next_url',function(){
  //	it('starts with the correct url',function(){
  //  	expect(next_url).toContain('http://psd-dev.internal.sanger.ac.uk');
  //	});
  //:w
//});
// expect every function named in init() to be defined somewhere
});
