describe("Application", function() {
	describe('url_root',function(){
  	it('is correct url',function(){
    	expect(url_root).toBe('http://psd-dev.internal.sanger.ac.uk');
// expect(next_url).toStartWith(url_root)

// expect every function named in init() to be defined somewhere

  	});
  });
});
